{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "npm install --no-audit --no-fund --omit=dev && cd server && npm install --no-audit --no-fund --omit=optional --omit=dev && cd .. && npm --prefix client install --no-audit --no-fund --omit=optional && npm --prefix client run build",
  "outputDirectory": null,
  "installCommand": "echo skip-install",
  "functions": {
    "api/index.js": {
      "runtime": "@vercel/node@3",
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index.js" }
  ]
}
